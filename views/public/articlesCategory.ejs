<%- include('../partials/header.ejs'); -%>
<%-include('../partials/site-navbar.ejs'); -%>

<body>
  <div class="container">
    <div class="jumbotron">
      <h1 class="display-4"><%= category.title %></h1>
      <p class="lead">Blog simples sobre assuntos gerais.</p>
    </div>
    <hr />
    <% if (articles.length > 0) { articles.forEach(article => { %>
    <div class="card">
      <div class="card-header">
        <h2><%= article.title %></h2>
      </div>
      <div class="card-body">
        <a href="/<%= article.slug %>" class="btn btn-primary">Ver artigo</a>
      </div>
    </div>
    <br />
    <% }) } else { %>
    <p>Ainda não existem artigos cadastrados para essa categoria.</p>
    <% } %>
  </div>
</body>

<%- include('../partials/footer.ejs'); -%>
